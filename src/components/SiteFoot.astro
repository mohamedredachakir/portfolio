<footer class="site-foot">
  <div class="site-foot__logo">
    <a href="/">
      <svg
        width="280"
        height="280"
        viewBox="0 0 280 280"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          d="M40 40 v200 h40 v-140 l60 140 l60 -140 v140 h40 v-200 h-60 l-40 100 l-40 -100 h-60 z"
          fill="#160000"
        >
        </path>
      </svg>

      <span class="u-sr-only">Mohamed Reda Chakir</span>
    </a>
  </div><!-- .site-foot__logo -->
</footer>

<style lang="scss">
  .site-foot {
    @include flex(row, center, center);

    position: relative;
    z-index: 3;

    padding: 1.5rem;

    border-top: 1px solid color(secondary);

    @include mq(phone) {
      padding: 1rem;
    }

    &__logo {
      svg {
        display: block;
        width: 3rem;
        height: 3rem;

        @include mq(phone) {
          width: 1.875rem;
          height: 1.875rem;
        }
      }
    }
  }
</style>
